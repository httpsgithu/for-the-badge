export interface IconConfig
{
    slug: string;
    color?: string;
    size?: number;
    position?: "left" | "right";
}

export interface IconData
{
    slug: string;
    title: string;
    hex: string;
    path: string;
}
